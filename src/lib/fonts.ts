import { Urbanist, Concert_One, Cherry_Bomb_One } from 'next/font/google'

export const roboto = Urbanist({ subsets: ['latin'], weight: ['100', '400', '700'] });

export const concert_one = Concert_One({ subsets: ['latin'], weight: '400', });
export const cherry = Cherry_Bomb_One({ subsets: ['latin'], weight: '400', });